/**
 * A module whose stylesheet Tailwind generates from the classes its sources name, which is how the
 * Tailwind processor enters the reproducibility matrix
 */
export const classes = 'text-red-600 font-bold';
