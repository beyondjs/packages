# Packages tests: index

Every validation of this repository, where it runs from and what it needs. `tests/` holds integration validations of the Packages implementation, one directory per group with its own entry (`index.mjs`) and report (`PASS`/`FAIL` per step, `N/N passed` at the end, exit code 1 on a failure). They are numbered steps rather than `*.test.mjs` files because each group drives one Engine-served implementation, a live service or a browser through one ordered scenario, and its README says what each step establishes. Contract tests of the service, the bootstrap and the development contract use Node's test runner and are listed [at the end](#nodetest-suites).

Unless a group says otherwise, a group runs from the Packages directory under BEE Node, with the bootstrap Engine serving this implementation, as described in [the stage-1 prerequisites](stage-1/README.md):

```sh
cd "$PACKAGES_DIR"
BEE_URL=http://localhost:1112 [WATCHERS_URL=http://localhost:1120] \
  node --import "$BEE_NODE_DIR/register.mjs" tests/<group>/index.mjs
```

Behavior of an installed toolchain is accepted by the command line's acceptance, never by these groups: they run against a checkout served by Engine.

## Layout

- `tests/<group>/fixtures/` (or `fixture/` in the groups that predate the suite rule) holds permanent source examples: packages, modules and assets as ordinary files. A harness copies them into a unique temporary directory, applies any narrow substitution there, and never writes the checked-in files. Each group's README describes its fixtures, their entry modules and their intentionally invalid cases.
- The `.mjs` files beside them are the harness and the assertions. Sources that stay generated or inline (size stress, deliberately malformed inputs, short edits) are listed with their reason in the group's README.
- Fixtures are not packages of this repository: `beyond.json` names only the root package, whose modules are discovered under `modules/`, so nothing under `tests/` enters its compilation. `npm pack` has no `files` rule or `.npmignore` here, so it follows `.gitignore` and includes all of `tests/`, fixtures and the one checked-in `node_modules` fixture directory included, in the archive the command line's acceptance installs; nothing there discovers, compiles or runs them.
- Generated output (`.artifacts/`, `.beyond/`, temporary directories) is never checked in.

## Groups

| Group | Validates | Entry and command | Prerequisites beyond Engine and BEE Node | Fixtures | Documentation and expected result |
| --- | --- | --- | --- | --- | --- |
| [cdn-analysis](cdn-analysis) | Publication forms, npm `exports` resolution and the `beyond-inventory/1` traced over a pinned graph | `index.mjs` | Optional `CDN_DIR` or `CDN_CONTRACTS_DIR` for the contract steps; optional `BEYOND_ESBUILD` | [`cdn-analysis/fixtures`](cdn-analysis/fixtures) (store also used by cdn-outputs and by CDN's delivery tests) | [README](cdn-analysis/README.md): `12/12 passed`, `9/9` without the contracts |
| [cdn-outputs](cdn-outputs) | Generated outputs in `esm` and `System.register`, distributions, keys, reproducibility, real React units, and `/styles/` and `/assets/` HTTP delivery | `index.mjs` | `BEYOND_ESBUILD`; optional `CDN_REACT_MODULES` | The cdn-analysis store and [`cdn-outputs/fixtures`](cdn-outputs/fixtures) | [README](cdn-outputs/README.md): `21/21 passed`, `17/17` without React packages |
| [cdn-diagnostics](cdn-diagnostics) | Semantic TypeScript diagnostics of one public module, its limits and what results disclose | `index.mjs` | `WATCHERS_URL` (no watcher is started) | [`cdn-diagnostics/fixtures/workspace`](cdn-diagnostics/fixtures/workspace); one generated in-memory stress source | [README](cdn-diagnostics/README.md): `24/24 passed` |
| [cdn-resolution](cdn-resolution) | Resolution of a dependency graph against npm-compatible registries | `index.mjs` | `WATCHERS_URL`; optional `CDN_DIR`/`CDN_CONTRACTS_DIR` | Package metadata and archives generated by the test registry ([registry.mjs](cdn-resolution/registry.mjs)) | [CDN resolution guide](../docs/cdn-resolution.md): `N/N passed` |
| [cdn-sources](cdn-sources) | Fetching and verifying the archives of a pinned graph into a store | `index.mjs` | As cdn-resolution | Packages published to the test registry by [fixtures.mjs](cdn-sources/fixtures.mjs), including archives built to be refused | [CDN resolution guide](../docs/cdn-resolution.md): `N/N passed` |
| [cli-baseline](cli-baseline) | Artifact guarantees, authoring forms and selectors for local execution | `index.mjs [defects\|declarations\|selection]` | None (no watcher) | [`cli-baseline/fixtures`](cli-baseline/fixtures) | [README](cli-baseline/README.md) and [local execution guide](../docs/local-cli-baseline.md): `22/22 steps passed` |
| [compiler](compiler) | A compiler child process that ends is reported and released; `oom.mjs` under a real memory ceiling | `index.mjs`, `oom.mjs` | `WATCHERS_URL`; a cgroup for `oom.mjs` | [`compiler/fixture`](compiler/fixture) | [README](compiler/README.md): `3/3 steps passed` |
| [development](development) | The development contract: files, service grants and events, preview selection | `index.mjs [files\|service\|preview]` | None | Temporary workspaces written by [harness.mjs](development/harness.mjs) | [Development contract](../docs/development-contract.md#implementation-and-limits) |
| [esbuild-packaging](esbuild-packaging) | The esbuild packaging mode compiled by Packages | `index.mjs` | `WATCHERS_URL`, `BEYOND_ESBUILD` | See its README | [README](esbuild-packaging/README.md): `8/8 steps passed` |
| [hello](hello) | Loader access to compiled implementation modules over HTTP ESM (not target compilation) | `index.mjs` | None | None | [Development guide](../docs/development.md) |
| [identity](identity) | Generated identifiers follow the identity of a source, not its location; an installed package is compiled under the installation it came from | `identity.test.mjs`, `installed.test.mjs` with Node's test runner (`node --import "$BEE_NODE_DIR/register.mjs" tests/identity/<file>`); harness in `identity/support/` | `WATCHERS_URL`, `BEYOND_MODULES`, `BEYOND_PLAYWRIGHT`; those of [preparation](preparation/README.md) | [`identity/fixtures`](identity/fixtures/README.md) | [identity/README.md](identity/README.md) |
| [preparation](preparation) | A package prepared by the compilation contract it declares, loaded in a browser | `index.mjs [<output directory>]` | `WATCHERS_URL`, `BEYOND_MODULES`, `BEYOND_PLAYWRIGHT`, Chrome | [`preparation/fixture`](preparation/fixture) | [README](preparation/README.md): `13/13 steps passed` |
| [preview](preview) | Development selection, the preview entry document and updates in a real browser; `template.mjs` a project created from the Workspace template with an installed toolchain | `index.mjs`, `cdn.mjs <directory>`, `template.mjs` (plain Node) | `WATCHERS_URL`, `BEYOND_ESBUILD`, `BEYOND_PLAYWRIGHT`; `template.mjs` needs `BEYOND_TEMPLATE`, `BEYOND_TOOLCHAIN`, `BEYOND_TEST_CDN_DIRECTORY` | [`preview/fixture`](preview/fixture); the template checkout | [README](preview/README.md): `9/9` and `10/10 steps passed` |
| [sharing](sharing) | One state behind the public subpaths of an npm package that share it: carrier and facades | `index.mjs` | Optional `BEYOND_ESBUILD` | [`sharing/fixtures`](sharing/fixtures) | [README](sharing/README.md): `12/12 steps passed` |
| [stage-1](stage-1) | Two workspace packages compiled, executed and regenerated after real edits through the watchers service | `index.mjs` | `WATCHERS_URL` | A temporary copy of the suite's `testbed/module-updates` (`BEYOND_TESTBED`) | [README](stage-1/README.md): `21/21 steps passed` |
| [styles-sdk](styles-sdk) | Stylesheets, Vue and Svelte sources, declarations and watched invalidation | `index.mjs` | `WATCHERS_URL` and the style and framework compilers installed | [`styles-sdk/fixture`](styles-sdk/fixture) | [README](styles-sdk/README.md): `14/14 steps passed` |
| [unified-runtime](unified-runtime) | Modules composed on the development runtime and updates reaching a running consumer | `index.mjs`, `service.mjs` | `WATCHERS_URL`, `BEYOND_ESBUILD` | [`unified-runtime/fixture`](unified-runtime/fixture) | [README](unified-runtime/README.md): `6/6` and `7/7 steps passed` |
| [utilities](utilities) | Which utilities the implementation runs on, and that updates keep arriving | `index.mjs` | `WATCHERS_URL`; `BEE_URL` also naming the utility servers when selected locally | [`utilities/fixture`](utilities/fixture) | [README](utilities/README.md): `18/18 steps passed` per round |

### Legacy diagnostic scripts

`test-bundlers`, `test-dependencies`, `test-package-install`, `test-packument`, `test-persistence`, `test-providers`, `test-server`, `test-settings` and `test-workspace` are legacy diagnostic scripts from earlier Packages designs. No command, README or validation runs them, their results are not evidence of current behavior, and they are kept unmodified as reference.

## node:test suites

| Suite | Validates | Command |
| --- | --- | --- |
| `service/test` | The development service's context, connection, extensions and implementation selection | `node --test "service/test/*.test.mjs"` ([service guide](../docs/service.md)) |
| `bootstrap/test` | The transitional Engine bootstrap: process groups and ports | `(cd bootstrap && npm test)` |
| `contracts/development/test` | The development contract's schema against its fixtures in `contracts/development/fixtures` | `node --test contracts/development/test/fixtures.test.mjs` ([development contract](../docs/development-contract.md)); it imports `ajv`, which this repository does not declare |

## Test organization and source fixtures

These rules are shared by every Beyond repository.

- Contract/unit and integration tests live in `test/` or `tests/`; complete journeys against an installed, composed or exported product live in `acceptance/`, with a README of their own. Harness infrastructure (servers, registries, process lifecycle, copying and substitution) lives in a `support/` directory of the consuming area.
- Applications, packages, modules, documents and assets a test exercises are checked-in files with their real extensions and directory structure under the consuming area's `fixtures/`. Each fixture group has a README naming its purpose, entry modules, the tests that use it, their command, the expected behavior and any intentionally invalid part. A reader inspects the example without running or decoding a generator.
- A harness copies the fixtures it runs or edits to a unique temporary directory, substitutes only explicit values such as versions, ports or origins, and never writes the checked-in files, even when a run fails. Credentials, machine paths and build output are never fixture source.
- Small input values, expected values, protocol payloads and short edits stay inline. Source is generated only when generation is the behavior under test (size or memory stress, combinations, deliberately malformed input); the guide states why, the parameters that reproduce it and how to inspect what was generated.
- Fixtures stay out of the repository's production compilation, discovery and packaging.
- Migrating a test preserves its scenario identities, its positive, negative and recovery cases and its real execution path; an existing failure stays reported as a failure.
