/************
Processor: ts
************/

// entities/collection.ts
declare namespace ns_0 {
  export interface ICollection<IItemData> {
    get(params: {
      id: string;
    }): Promise<IItemData | void>;
    set(params: {
      id: string;
      data: IItemData;
    }): Promise<void>;
    delete(params: {
      id: string;
    }): Promise<void>;
  }
}


// entities/packages.ts
declare namespace ns_1 {
  import ICollection = ns_0.ICollection;
  export type PackageSource = 'npm' | 'github' | 'github-pkg' | 'gitlab' | 'artifactory' | 'url';
  export interface IPackageData {
    key: string;
    source: PackageSource;
    name: string;
    version?: string;
    owner?: string;
    repo?: string;
    ref?: string;
    domain?: string;
    timestamps: {
      created: number;
      updated?: number;
    };
  }
  export type Packages = ICollection<IPackageData>;
}


// storage/file.ts
declare namespace ns_2 {
  /// <reference types="@types/node" />
  /// <reference types="@types/node" />
  export interface IFileStorage {
    stream(path: string): Promise<NodeJS.WritableStream>;
    load(path: string): Promise<Buffer>;
    exists(path: string): Promise<boolean>;
    delete(path: string): Promise<void>;
  }
}


export import ICollection = ns_0.ICollection;
export import PackageSource = ns_1.PackageSource;
export import IPackageData = ns_1.IPackageData;
export import Packages = ns_1.Packages;
export import IFileStorage = ns_2.IFileStorage;

export declare const hmr: {on: (event: string, listener: any) => void, off: (event: string, listener: any) => void };