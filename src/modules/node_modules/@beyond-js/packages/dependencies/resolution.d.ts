/************
Processor: ts
************/

import * as __beyond_dep_ns_0 from '@beyond-js/packages/repositories/types';
// git.ts
declare namespace ns_0 {
  import IPackageResolution = __beyond_dep_ns_0.IPackageResolution;
  import RepositoryType = __beyond_dep_ns_0.RepositoryType;
  /**
   * Parses git-based dependency specifiers (e.g., git+https://..., github:user/repo).
   * Identifies the host, repository, owner, and optional ref (branch, tag, or commit).
   */
  export class GitParser {
    static known: Record<string, RepositoryType>;
    static parse(version: string): (IPackageResolution['git'] & {
      repository: RepositoryType;
    }) | null;
  }
}


// index.ts
declare namespace ns_1 {
  import IPackageResolution = __beyond_dep_ns_0.IPackageResolution;
  import RepositoryType = __beyond_dep_ns_0.RepositoryType;
  import PackageResolutionType = __beyond_dep_ns_0.PackageResolutionType;
  /**
   * Resolves and interprets a dependency version specifier declared in a package.json.
   * Determines the resolution type (semver, tarball, git, etc.) and extracts relevant metadata.
   */
  export class PackageResolution implements IPackageResolution {
    #private;
    get name(): string;
    get scope(): string;
    get version(): string;
    get resolution(): PackageResolutionType;
    get semver(): string;
    get repository(): RepositoryType;
    get git(): {
      host: string;
      owner: string;
      repo: string;
      ref?: string;
    };
    get error(): {
      code: string;
      text: string;
    };
    constructor(name: string, version: string);
  }
}


// semver.ts
declare namespace ns_2 {
  /**
   * Utility to detect whether a version string follows a basic semver pattern.
   * Note: This is a simplified check and should be replaced by a proper semver library if needed.
   */
  export class Semver {
    static is(version: string): boolean;
  }
}



export declare const hmr: {on: (event: string, listener: any) => void, off: (event: string, listener: any) => void };