/************
Processor: ts
************/

// hash.ts
declare namespace ns_0 {
  import DependenciesSpecs = ns_1.DependenciesSpecs;
  export class _default {
    #private;
    get value(): number;
    constructor(specs: DependenciesSpecs);
    /**
     * Generates a CRC32 hash for the given dependencies specifications.
     * @param specs The dependencies specifications to hash.
     * @returns The CRC32 hash as a string.
     */
    update(): void;
  }
}


// index.ts
declare namespace ns_1 {
  interface IDependencies {
    dependencies?: unknown;
    devDependencies?: unknown;
    peerDependencies?: unknown;
    optionalDependencies?: unknown;
    overrides?: Record<string, string | Record<string, string>>;
  }
  type Kind = 'main' | 'development' | 'peer' | 'optional';
  /**
   * Represents a structured list of dependencies from a `package.json`.
   * Maps each dependency name (key) to:
   * - a version (semver, git ref, URL, etc.)
   * - its kind: 'main', 'development', 'peer', or 'optional'
   *
   * Also stores:
   * - raw `overrides`
   * - a list of warnings if the input structure is invalid
   */
  export class DependenciesSpecs extends Map<string, {
    version: string;
    kind: Kind;
  }> {
    #private;
    get hash(): number;
    get overrides(): Record<string, string | Record<string, string>> | undefined;
    get warnings(): string[];
    constructor(json: IDependencies);
  }
  export {};
}


export import DependenciesSpecs = ns_1.DependenciesSpecs;

export declare const hmr: {on: (event: string, listener: any) => void, off: (event: string, listener: any) => void };